Model: Gemini 3.1 Pro High
Runner: Antigravity
Date: 2026-08-25
Repository commit: 03bc584b9a4e03aa8eca51b872435d07b4e7009f
Prompt ID: K2

# K2 — State-transition and invariant audit

| Flow | Starting State | State Transitions | Persisted Fields | Visible Outcome | Invariant | File:Line Evidence |
|---|---|---|---|---|---|---|
| 1. UPC input -> product lookup -> basket -> benefit balance. | User is logged in, empty scanner input | `_busy=true` -> lookup via `AplService` -> `_lastInfo` populated -> `AppState.addItem` creates line -> updates `balances` `used` count | `basket` (new line), `balances` (used count incremented), `updatedAt` (timestamp) | Item details shown on screen; WIC benefit balance progress bar increases. | WIC category `used` must never exceed `allowed` if a limit exists (handled by blocking Add if cap reached). | `Project3/lib/screens/scan_screen.dart:188` (EVIDENCE), `Project3/lib/state/app_state.dart:398` (EVIDENCE) |
| 2. Basket quantity at category cap -> paid overflow -> decrement. | `balances[cat].used == allowed` | `incrementItem` fails `_canAddCanon` check -> creates/increments `PAID` category line -> `decrementItem` targets `PAID` line first. | `basket` (new/modified PAID line), `balances` (PAID used count updated) | PAID item quantity increases in basket; original WIC category shows max usage. Decrementing reduces PAID qty. | `decrementItem` must always prioritize deducting from `PAID` before WIC categories. | `Project3/lib/state/app_state.dart:452` (EVIDENCE), `Project3/lib/state/app_state.dart:513` (EVIDENCE) |
| 3. Receipt image -> OCR text -> UPC matches -> basket. | Empty `_foundItems`, `_scanning=false` | `_scanning=true` -> OCR parses image to text -> Regex finds 12-14 digit strings -> matched strings checked in `AplService` -> `_foundItems` populated. | `basket` appended via `_addAllToBasket`, `balances` updated via `addItem` | List of matched items with green checkmarks appears; items flow into cart. | Only items returning non-null from `findByUpc` are ever shown in `_foundItems`. | `Project3/lib/screens/receipt_scanner_screen.dart:125` (EVIDENCE), `Project3/lib/screens/receipt_scanner_screen.dart:152` (EVIDENCE) |
| 4. Basket -> QR handoff -> finish transaction. | Populated `basket`, `appState.balances` matching usage | JSON encode `appState.basket` -> Render QR -> `checkout()` clears basket. | `basket` (cleared array), `updatedAt` | QR code rendered; pressing finish clears cart and returns user to scan screen. | `used` counts must remain persistent to track monthly usage while `basket` resets to empty. | `Project3/lib/screens/qr_checkout_screen.dart:14` (EVIDENCE), `Project3/lib/state/app_state.dart:559` (EVIDENCE) |
| 5. Sign-in -> restore state -> new-month behavior. | Null `_uid`, `_balancesLoaded=false` | `updateUser(User)` triggers `loadUserState` -> fetch doc -> check `_isNewMonth` -> `_resetMonthlyUsage`. | `balances` (used fields=0), `basket` (cleared), `updatedAt` | UI loads and balances screen shows 0 usage for the new month. | If `updatedAt` is from a prior month, `balances` and `basket` must be wiped before any UI renders. | `Project3/lib/state/app_state.dart:284` (EVIDENCE), `Project3/lib/state/app_state.dart:205` (EVIDENCE) |

## Contradictions and Silent-Failure Paths

- **Silent Failure Path (Receipt Parsing):** If the OCR API returns a successful 200 response but cannot read any text (empty `ParsedText`), `_parseUPCs` silently does not find any UPCs and shows "No UPC candidates found". (INFERENCE)
- **Contradiction (Paid Overflow):** The scan screen `_addToBasket` blocks adding the initially scanned item if the category is capped (`appState.canAdd(category)` returns false). However, `incrementItem` in the basket allows you to surpass the cap by dynamically creating a "PAID" category. This is a contradictory workflow where adding an item from the scan screen is blocked, but incrementing it in the basket is allowed. (EVIDENCE - `Project3/lib/state/app_state.dart:370` and `Project3/lib/state/app_state.dart:452`)
- **Silent Failure Path (QR Checkout Cancellation):** `QRCheckoutScreen` does not clear the cart if the user navigates back natively using the App Bar instead of the dialog button. However, `BasketScreen._showQRDialog` contains code for a dialog checkout that *was* used previously, but `BasketScreen` now routes to `QRCheckoutScreen`. (EVIDENCE)
- **Silent Failure Path (Firestore Offline):** If `loadUserState` fails to fetch due to network issues, it does not throw an error to the UI and leaves `_balancesLoaded` in an undefined or silently broken state, as the `try-finally` block does not `catch` the exception to update the UI gracefully. (EVIDENCE - `Project3/lib/state/app_state.dart:290`)
